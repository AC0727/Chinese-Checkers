package GameBoard;

/**
 * BoardPositionState
 * @author AdeleChen
 * the state of a BoardPosition. It can either be empty or filled with a piece.
 */

public enum BoardPositionState {
        GREEN, RED, YELLOW, EMPTY
}
